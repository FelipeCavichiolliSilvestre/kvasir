<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="utf-8" />
  {{>bootstrap }}
  <title>Pacientes</title>
</head>
<body>
{{>navbar}}
<div class="container">
  <div class="row justify-content-between">
    {{message}}
    <form class="col-12 col-sm-6 col-md-4">
      <div class="input-group mb-3">
        <input class="form-control" name="search" value="{{search}}" type="search" maxlength="255" minlength="2" />
        <input class="input-group-text" type="submit" value="🔍">
      </div>
    </form>

    <div class="text-end col-12 col-sm-6 col-md-4">
      <a class="btn btn-primary" href="/patients/register">
        Registrar Paciente
      </a>
    </div>
  </div>
  <p>{{help}}</p>

  {{#if isEmpty}}
    <p class="text-center">Nenhum paciente encontrado</p>
  {{else}}
    <table class="table">
      <tr>
        <th>Id</th>
        <th>Paciente</th>
        <th>Ações</th>
      </tr>
      {{#each patients}}
        <tr>
          <td>{{this.id}}</td>
          <td>{{this.name}}</td>
          <td>
            <a href="/patients/{{this.id}}/view" type="submit">Detalhes</a>
          </td>
        </tr>
      {{/each}}
    </table>
  {{/if}}
</div>
</body>
</html>